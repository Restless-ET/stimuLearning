generator:
  class: jRollerDoctrineGenerator
  param:
    model_class:           Service
    theme:                 jroller
    non_verbose_templates: true
    with_show:             false
    singular:              ~
    plural:                ~
    route_prefix:          service
    with_doctrine_route:   true
    actions_base_class:    sfActions

    config:
      actions: ~
      fields: ~

      list:
        title: 'Services'
        display: [ setup_fee, cost_per_user, CAPEX_percentage, tick_to_apply ]

      filter:
        class: false

      form:    ~
      edit:    ~
      new:     ~
      show:    ~
